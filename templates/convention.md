# convention: {{RULE_ID}}

<!-- Glob-routed good-practice the editing agent reads on match. Stable id for
     cross-boundary `overrides: {{RULE_ID}}` markers. Project conventions = delta over harness. -->

**Applies:** {{GLOB}}              <!-- e.g. **/*.tsx -->
**Rule:** {{RULE}}                 <!-- terse, actionable, what opus gets wrong without it -->
**Why:** {{RATIONALE}}
