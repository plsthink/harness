# Context map — {{PROJECT_NAME}}

Navigation spine: routes root glossary → per-package glossaries. Multi-package only.

- root → `docs/CONTEXT.md`
<!-- one routing row per package the repo ships — duplicate the line below for each -->
- {{PACKAGE}} → `packages/{{PACKAGE}}/docs/CONTEXT.md`
