{
  "name": "slug-mono",
  "version": "0.0.0",
  "private": true,
  "description": "Harness multi-package mock fixture: a slug library + CLI in an npm-workspaces monorepo.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "node --test"
  }
}
