{
  "name": "cms-mono",
  "version": "0.0.0",
  "private": true,
  "description": "Harness multi-package mock fixture: a tiny markdown CMS — core parser, render-route HTTP server, build CLI — in an npm-workspaces monorepo.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "node --test"
  }
}
