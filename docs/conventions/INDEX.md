# Conventions routing index — harness project (delta over global)

Project-delta conventions for the harness's OWN code, loaded before editing matching files
. Delta over `${CLAUDE_PLUGIN_ROOT}/conventions/` — never restates a global rule.

**Starts empty** (lean-first): fills only if the harness catches itself misbehaving in its own
code. The authoring standard is NOT here — it's `${CLAUDE_PLUGIN_ROOT}/shared/authoring-standard.md`
(a named-skill reference, not glob-routed good-practice).

| Matcher | Load (rule-ids) |
|---|---|
| _(none yet)_ | |
